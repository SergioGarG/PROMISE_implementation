robot1[<> (l1) && <> (l2) && [] ((l1) -> X ((!l1) W (l2))) && [] ((l2) -> X ((!l2) W (l1))), eh, [] (<> (lcharge) && <> (l4)), fb, cond]

	eh_default[<> (l1)]

	eh_e1[eh]

		eh_default[<> (l2)]

		eh_e2[(X a)]

	fb_1[<> (l1)]

	fb_2[<> (l2)]

	cond_e1[<> (l1)]

	cond_e2[<> (l2)]


